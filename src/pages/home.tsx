/** @jsx jsx */ jsx;
import { jsx } from '@emotion/core';
import { colors } from '../styles';

export default () => (
  <div
    css={{
      background: colors.shrekGreen,
    }}
  >
    야호
  </div>
);
